numpy>=1.24
scipy>=1.10
trimesh>=4.0
open3d>=0.18
fastapi>=0.100
uvicorn>=0.23
python-multipart>=0.0.6
# pythonocc-core is NOT installable via pip — it lives in its own conda
# env for Phase C (fillet / chamfer / STEP export). See README.md step 2.
